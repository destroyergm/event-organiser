<div id="user_header">
	<h1>List of all events</h1>
</div>

<ul>
<% @events.each do |event| %>
	<li><%= event.place %> | <%= event.date %> By <%= event.creator.name %> | <%= link_to "Show", event_path(event) %></li>
<% end %>
</ul>
<%= link_to "Back to Index", root_url %>